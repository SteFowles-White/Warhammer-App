import React from "react";

const HexContainer = (props) => {

    const HexContainerClasses = `hex__container {props.hex_row}`
    
    return (
        <div className={HexContainerClasses}> 

        </div>
    )
}

export default HexContainer;